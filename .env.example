# Copy to .env and populate with newly generated credentials. Never commit .env.
SARVAM_API_KEY=
GEMINI_API_KEY=
# Optional fallback provider; leave empty unless using the Anthropic backend.
ANTHROPIC_API_KEY=
